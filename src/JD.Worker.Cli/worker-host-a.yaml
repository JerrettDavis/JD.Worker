worker:
  id: host-agent-01
  labels:
    execution: host
    runtime: direct
  pools:
    - name: default
      concurrency: 2
  workspace:
    root: ./workspaces/host-a
    cleanup: immediate
  sandbox: none
cnc:
  - name: orchestrator
    type: Http
    settings:
      baseUrl: ${JD_WORKER_API_URL}
      pollIntervalSeconds: 2
      leaseSeconds: 300
